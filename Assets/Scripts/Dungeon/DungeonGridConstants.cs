/// <summary>
/// Constant is shared by FloorLayout.cs, RoomConnector.cs and DungeonManager.cs
/// </summary>
public static class DungeonGridConstants
{
    /// <summary>
    /// Empty space between rooms in the grid, in tiles. This is the minimum distance between rooms, so that they don't overlap.
    /// </summary>
    public const int RoomGap = 4;
}